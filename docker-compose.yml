services:
  app:
    build: .
    container_name: open-api-erp
    restart: always
    ports:
      - "${PORT:-11122}:${PORT:-11122}"
    volumes:
      - .:/app
      - /app/node_modules
    env_file:
      - .env
